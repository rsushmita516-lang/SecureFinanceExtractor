{
  "name": "@vessify/db",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "generate": "node scripts/prisma-with-env.mjs generate",
    "migrate": "node scripts/prisma-with-env.mjs migrate dev",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "build": "tsc -p tsconfig.json",
    "lint": "echo \"No lint configured for db\""
  },
  "dependencies": {
    "@prisma/client": "^6.16.2"
  },
  "devDependencies": {
    "@types/node": "^20.19.43",
    "prisma": "^6.16.2",
    "typescript": "^5.9.3"
  }
}
